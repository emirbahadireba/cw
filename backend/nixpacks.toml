# Railway Nixpacks Configuration
[phases.setup]
nixPkgs = ["nodejs-18_x", "npm-8_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run migrate || true"]

[start]
cmd = "npm start"

